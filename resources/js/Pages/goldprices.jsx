import { Table } from "@/Components/Gold";
import MainContent from "@/Components/MainContent";
import Layout from "@/Layouts/Layout";
import React from "react";

const GoldPrice = () => {
    return (
        <Layout>
            <MainContent>
                <Table />
            </MainContent>
        </Layout>
    );
};

export default GoldPrice;
